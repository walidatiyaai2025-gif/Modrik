<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Install MODRIK</title><style>body{font-family:system-ui;background:#f8fafc;color:#0f172a;margin:0}.wrap{max-width:760px;margin:3rem auto;padding:1rem}.card{background:white;border:1px solid #e2e8f0;border-radius:18px;padding:1.5rem;box-shadow:0 8px 25px #0f172a12;margin-bottom:1rem}.grid{display:grid;grid-template-columns:1fr 1fr;gap:1rem}label{display:block;font-weight:600;font-size:.9rem}input{width:100%;box-sizing:border-box;padding:.7rem;border:1px solid #cbd5e1;border-radius:8px;margin-top:.3rem}button{background:#25546f;color:white;border:0;border-radius:9px;padding:.8rem 1.2rem;font-weight:700}.ok{color:#15803d}.bad,.errors{color:#b91c1c}@media(max-width:650px){.grid{grid-template-columns:1fr}}</style></head><body><main class="wrap"><h1>MODRIK Setup Wizard</h1><p>Welcome → Requirements → Database → URLs / Runtime → Admin Account → Review → Install → Finish</p><section class="card"><h2>Server requirements</h2>@foreach($requirements as $name=>$ok)<p class="{{ $ok?'ok':'bad' }}">{{ $ok?'✓':'✕' }} {{ str_replace('_',' ',$name) }}</p>@endforeach</section><form method="post" action="{{ route('installer.submit') }}" class="card">@csrf<h2>Installation details</h2>@if($errors->any())<div class="errors">@foreach($errors->all() as $error)<p>{{ $error }}</p>@endforeach</div>@endif<div class="grid"><label>Database host<input name="db_host" value="{{ old('db_host','127.0.0.1') }}" required></label><label>Database port<input name="db_port" type="number" value="{{ old('db_port',3306) }}" required></label><label>Database name<input name="db_database" value="{{ old('db_database') }}" required></label><label>Database user<input name="db_username" value="{{ old('db_username') }}" required></label><label>Database password<input name="db_password" type="password" autocomplete="new-password" required></label><label>Backend URL<input name="app_url" type="url" value="{{ old('app_url') }}" required></label><label>Web URL<input name="web_url" type="url" value="{{ old('web_url') }}" required></label><label>Admin email<input name="admin_email" type="email" value="{{ old('admin_email') }}" required></label><label>Admin password<input name="admin_password" type="password" minlength="12" autocomplete="new-password" required></label><label>Release SHA<input name="release_sha" pattern="[0-9a-fA-F]{40}" value="{{ old('release_sha') }}" required></label></div><p>No credential is retained unless final installation succeeds. Existing .env files are never overwritten.</p><button type="submit">Review and install</button></form></main></body></html>
