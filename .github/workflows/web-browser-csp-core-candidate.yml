name: Web Browser CSP Core Candidate

on:
  pull_request:
    paths:
      - "qa/web-e2e/**"
      - ".github/workflows/web-browser-csp-core-candidate.yml"
      - ".github/workflows/web-browser-runtime-e2e.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: web-browser-csp-core-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  pr-118-core-browser:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Check out exact Issue #108 harness head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          path: harness

      - name: Check out canonical CSP repair candidate
        uses: actions/checkout@v4
        with:
          ref: refs/pull/118/head
          path: target

      - name: Resolve exact provenance
        id: provenance
        shell: bash
        run: |
          set -euo pipefail
          echo "harness_sha=$(git -C harness rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "target_sha=$(git -C target rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2
          cache: npm
          cache-dependency-path: target/apps/web/package-lock.json

      - name: Install Web candidate
        working-directory: target/apps/web
        run: npm ci

      - name: Cache Chromium runtime
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/ms-playwright
          key: playwright-1.62.1-chromium-${{ runner.os }}

      - name: Install pinned Playwright runtime
        shell: bash
        run: |
          set -euo pipefail
          npm install --prefix "$RUNNER_TEMP/playwright" --ignore-scripts --no-save playwright@1.62.1
          PLAYWRIGHT_BROWSERS_PATH="$RUNNER_TEMP/ms-playwright" \
            node "$RUNNER_TEMP/playwright/node_modules/playwright/cli.js" install --with-deps chromium

      - name: Build exact production candidate
        env:
          OBSERVED_SHA: ${{ steps.provenance.outputs.target_sha }}
        working-directory: target/apps/web
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .next
          export MODRIK_RUNTIME_INSPECTOR_ENABLED=false
          export MODRIK_RUNTIME_ENVIRONMENT=production
          export MODRIK_BUILD_VERSION=csp-core-browser
          export MODRIK_GIT_SHA="$OBSERVED_SHA"
          npm run build

      - name: Execute core browser acceptance against canonical repair
        env:
          NODE_PATH: ${{ runner.temp }}/playwright/node_modules
          PLAYWRIGHT_BROWSERS_PATH: ${{ runner.temp }}/ms-playwright
          MODRIK_E2E_TARGET_DIR: ${{ github.workspace }}/target
          MODRIK_E2E_EVIDENCE_DIR: ${{ github.workspace }}/evidence/csp-core-candidate
          MODRIK_E2E_CANDIDATE: pr-118-core
          MODRIK_E2E_PROFILE: core
          MODRIK_E2E_EXPECTED_SHA: ${{ steps.provenance.outputs.target_sha }}
          MODRIK_E2E_OBSERVED_SHA: ${{ steps.provenance.outputs.target_sha }}
        run: node harness/qa/web-e2e/browser-runtime-acceptance.cjs

      - name: Upload sanitized browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: web-browser-csp-core-candidate
          path: evidence/csp-core-candidate
          if-no-files-found: error
          retention-days: 14
