name: Issue 305 Apply Patch Once

on:
  push:
    branches:
      - issue-305-year-scoped-self-selection
    paths:
      - .github/workflows/issue-305-apply-patch-once.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: issue-305-year-scoped-self-selection
          fetch-depth: 0
      - name: Apply owner-authorized year-scoped self-selection contract
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re

          def write(path, content):
              Path(path).write_text(content, encoding='utf-8')

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              if old not in text:
                  raise SystemExit(f'missing expected block in {path}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')

          service = r'''<?php

          namespace App\Services;

          use App\Exceptions\ApiProblemException;
          use Illuminate\Database\Query\Builder;
          use Illuminate\Support\Facades\DB;
          use Illuminate\Support\Str;

          final class AcademicTrackCatalogueService
          {
              private const LOCALES = ['ar', 'en', 'fr'];

              /** @return list<array{id: string, year: array{key: string, label: string}, labels: array{ar: string, en: string, fr: string}}> */
              public function catalogue(): array
              {
                  $tracks = [];

                  foreach ($this->availableQuery()
                      ->orderBy('academic_tracks.year_level')
                      ->orderBy('academic_tracks.id')
                      ->get() as $row) {
                      $track = (array) $row;
                      $labels = $this->displayLabels($track['title'] ?? null);
                      $year = $this->displayYear($track['year_level'] ?? null);
                      if ($labels === null || $year === null) {
                          continue;
                      }

                      $tracks[] = [
                          'id' => (string) $track['id'],
                          'year' => $year,
                          'labels' => $labels,
                      ];
                  }

                  return $tracks;
              }

              /** @return array{id: string, year_level: string} */
              public function requireAvailableTrack(string $academicTrackId): array
              {
                  $row = $this->availableQuery()
                      ->where('academic_tracks.id', $academicTrackId)
                      ->first();

                  if ($row === null) {
                      throw $this->unavailableTrack();
                  }

                  $track = (array) $row;
                  if ($this->displayLabels($track['title'] ?? null) === null || $this->displayYear($track['year_level'] ?? null) === null) {
                      throw $this->unavailableTrack();
                  }

                  return [
                      'id' => (string) $track['id'],
                      'year_level' => (string) $track['year_level'],
                  ];
              }

              private function availableQuery(): Builder
              {
                  $query = DB::table('academic_tracks')
                      ->select([
                          'academic_tracks.id',
                          'academic_tracks.year_level',
                          'academic_tracks.title',
                          'academic_tracks.is_fixture',
                      ]);

                  if (! (bool) config('modrik.fixture.enabled')) {
                      $query->where('academic_tracks.is_fixture', false);
                  }

                  return $query;
              }

              /** @return null|array{key: string, label: string} */
              private function displayYear(mixed $value): ?array
              {
                  if (! is_string($value)) {
                      return null;
                  }

                  $key = trim($value);
                  if ($key === '' || mb_strlen($key) > 160 || $this->containsUnsafeMarkupOrControls($key)) {
                      return null;
                  }

                  $segments = preg_split('/[:\\/]+/', $key) ?: [$key];
                  if (Str::upper((string) ($segments[0] ?? '')) === 'YEAR') {
                      array_shift($segments);
                  }
                  if (count($segments) > 1 && preg_match('/^[A-F0-9]{8}$/i', (string) end($segments)) === 1) {
                      array_pop($segments);
                  }

                  $readable = trim(implode(' ', $segments));
                  $readable = str_replace(['-', '_', '.'], ' ', $readable);
                  $label = Str::headline($readable === '' ? $key : $readable);
                  if ($label === '' || mb_strlen($label) > 160 || $this->containsUnsafeMarkupOrControls($label)) {
                      return null;
                  }

                  return ['key' => $key, 'label' => $label];
              }

              /** @return null|array{ar: string, en: string, fr: string} */
              private function displayLabels(mixed $value): ?array
              {
                  if (is_string($value)) {
                      $decoded = json_decode($value, true);
                  } elseif (is_array($value)) {
                      $decoded = $value;
                  } else {
                      return null;
                  }

                  if (! is_array($decoded)) {
                      return null;
                  }

                  $labels = [];
                  foreach (self::LOCALES as $locale) {
                      $candidate = $decoded[$locale] ?? null;
                      if (! is_string($candidate)) {
                          return null;
                      }

                      $label = trim($candidate);
                      if ($label === '' || mb_strlen($label) > 160 || $this->containsUnsafeMarkupOrControls($label)) {
                          return null;
                      }

                      $labels[$locale] = $label;
                  }

                  return $labels;
              }

              private function containsUnsafeMarkupOrControls(string $label): bool
              {
                  if (strip_tags($label) !== $label) {
                      return true;
                  }

                  return preg_match('/[\\p{Cc}\\p{Cf}]/u', $label) === 1;
              }

              private function unavailableTrack(): ApiProblemException
              {
                  return new ApiProblemException(
                      404,
                      'RESOURCE_NOT_FOUND',
                      'Resource not found',
                      'The requested academic track is unavailable.',
                  );
              }
          }
          '''.replace('\n          ', '\n').lstrip()
          write('apps/backend/app/Services/AcademicTrackCatalogueService.php', service)

          replace_once(
              'apps/backend/app/Http/Controllers/Api/AcademicContextController.php',
              "        return ApiResponse::success($request, [\n            'tracks' => $this->catalogue->catalogue($this->user($request)),\n        ])->header('Cache-Control', 'no-store, private');",
              "        $this->user($request);\n\n        return ApiResponse::success($request, [\n            'tracks' => $this->catalogue->catalogue(),\n        ])->header('Cache-Control', 'no-store, private');",
          )

          p = Path('apps/backend/app/Services/AcademicContextService.php')
          text = p.read_text(encoding='utf-8')
          old = '$this->catalogue->requireAuthorizedTrack($user, $academicTrackId)'
          if text.count(old) != 2:
              raise SystemExit('expected two per-user authorization calls in AcademicContextService')
          p.write_text(text.replace(old, '$this->catalogue->requireAvailableTrack($academicTrackId)'), encoding='utf-8')

          backend_test = r'''<?php

          namespace Tests\Feature;

          use App\Models\User;
          use Database\Seeders\LearningSliceSeeder;
          use Illuminate\Foundation\Testing\RefreshDatabase;
          use Illuminate\Support\Facades\DB;
          use Tests\TestCase;

          class AcademicTrackCatalogueTest extends TestCase
          {
              use RefreshDatabase;

              private const TOKEN = 'modrik-academic-catalogue-fixture-token';

              protected function setUp(): void
              {
                  parent::setUp();

                  config([
                      'modrik.fixture.enabled' => true,
                      'modrik.fixture.bearer_token' => self::TOKEN,
                      'modrik.fixture.user_id' => LearningSliceSeeder::USER_ID,
                      'modrik.idempotency.secret' => 'test-only-idempotency-secret',
                  ]);
                  $this->seed(LearningSliceSeeder::class);
              }

              public function test_catalogue_requires_authentication_and_does_not_require_per_user_assignment(): void
              {
                  $this->getJson('/v1/academic-tracks')->assertUnauthorized()
                      ->assertJsonPath('code', 'AUTHENTICATION_REQUIRED');

                  DB::table('academic_track_authorizations')->delete();

                  $sameYearId = '01J00000000000000000000050';
                  $otherYearId = '01J00000000000000000000051';
                  $this->createTrack($sameYearId, 'FIXTURE:CATALOGUE:SAME-YEAR', 'FIXTURE-YEAR-6-7', [
                      'ar' => 'مسار ثانٍ لنفس السنة',
                      'en' => 'Second track for same year',
                      'fr' => 'Deuxième parcours de la même année',
                  ]);
                  $this->createTrack($otherYearId, 'FIXTURE:CATALOGUE:OTHER-YEAR', 'FIXTURE-YEAR-8', [
                      'ar' => 'مسار سنة أخرى',
                      'en' => 'Another year track',
                      'fr' => 'Parcours d’une autre année',
                  ]);

                  $response = $this->withToken(self::TOKEN)
                      ->getJson('/v1/academic-tracks')
                      ->assertOk()
                      ->assertHeader('Cache-Control', 'no-store, private');

                  $tracks = $response->json('data.tracks');
                  $this->assertIsArray($tracks);
                  $ids = array_column($tracks, 'id');
                  $this->assertContains(LearningSliceSeeder::TRACK_ID, $ids);
                  $this->assertContains($sameYearId, $ids);
                  $this->assertContains($otherYearId, $ids);

                  $sameYear = collect($tracks)->firstWhere('id', $sameYearId);
                  $this->assertSame(['id', 'year', 'labels'], array_keys($sameYear));
                  $this->assertSame(['key', 'label'], array_keys($sameYear['year']));
                  $this->assertSame('FIXTURE-YEAR-6-7', $sameYear['year']['key']);
                  $this->assertSame('Fixture Year 6 7', $sameYear['year']['label']);
                  $this->assertSame('Second track for same year', $sameYear['labels']['en']);
              }

              public function test_catalogue_exposes_shared_year_scope_for_all_tracks_in_that_year(): void
              {
                  DB::table('academic_track_authorizations')->delete();
                  $firstId = '01J00000000000000000000052';
                  $secondId = '01J00000000000000000000053';
                  foreach ([
                      [$firstId, 'A'],
                      [$secondId, 'B'],
                  ] as [$id, $suffix]) {
                      $this->createTrack($id, 'FIXTURE:YEAR:SCOPE:'.$suffix, 'YEAR:GRADE-6:ABCDEF12', [
                          'ar' => 'مسار السنة '.$suffix,
                          'en' => 'Year track '.$suffix,
                          'fr' => 'Parcours année '.$suffix,
                      ]);
                  }

                  $tracks = $this->withToken(self::TOKEN)
                      ->getJson('/v1/academic-tracks')
                      ->assertOk()
                      ->json('data.tracks');

                  $scoped = collect($tracks)->whereIn('id', [$firstId, $secondId])->values();
                  $this->assertCount(2, $scoped);
                  $this->assertSame(['YEAR:GRADE-6:ABCDEF12'], $scoped->pluck('year.key')->unique()->values()->all());
                  $this->assertSame(['Grade 6'], $scoped->pluck('year.label')->unique()->values()->all());
              }

              public function test_display_invalid_track_or_year_fails_closed(): void
              {
                  DB::table('academic_track_authorizations')->delete();
                  $missingLocaleId = '01J00000000000000000000054';
                  $unsafeYearId = '01J00000000000000000000055';
                  $markupId = '01J00000000000000000000056';

                  $this->createTrack($missingLocaleId, 'FIXTURE:INVALID:MISSING', 'FIXTURE-YEAR', [
                      'ar' => 'مسار ناقص',
                      'en' => 'Missing localization',
                  ]);
                  $this->createTrack($unsafeYearId, 'FIXTURE:INVALID:YEAR', "YEAR:GRADE-6\u{0007}", [
                      'ar' => 'مسار سنة غير آمنة',
                      'en' => 'Unsafe year',
                      'fr' => 'Année non sûre',
                  ]);
                  $this->createTrack($markupId, 'FIXTURE:INVALID:MARKUP', 'FIXTURE-YEAR', [
                      'ar' => 'مسار آمن',
                      'en' => '<b>Unsafe markup</b>',
                      'fr' => 'Balisage non sûr',
                  ]);

                  $tracks = $this->withToken(self::TOKEN)
                      ->getJson('/v1/academic-tracks')
                      ->assertOk()
                      ->json('data.tracks');

                  $ids = array_column($tracks, 'id');
                  $this->assertNotContains($missingLocaleId, $ids);
                  $this->assertNotContains($unsafeYearId, $ids);
                  $this->assertNotContains($markupId, $ids);
                  $this->assertContains(LearningSliceSeeder::TRACK_ID, $ids);
              }

              public function test_reset_accepts_a_display_safe_track_without_an_assignment_row(): void
              {
                  DB::table('academic_track_authorizations')->delete();
                  $targetId = '01J00000000000000000000057';
                  $this->createTrack($targetId, 'FIXTURE:SELF-SELECT:TARGET', 'YEAR:GRADE-7:1234ABCD', [
                      'ar' => 'مسار يختاره الطالب',
                      'en' => 'Learner selected track',
                      'fr' => 'Parcours choisi par l’apprenant',
                  ]);

                  $this->withToken(self::TOKEN)
                      ->withHeader('Idempotency-Key', 'year-self-select-reset-0001')
                      ->postJson('/v1/academic-context/reset', ['academic_track_id' => $targetId])
                      ->assertOk()
                      ->assertJsonPath('data.academic_track_id', $targetId)
                      ->assertJsonPath('data.year_level', 'YEAR:GRADE-7:1234ABCD');

                  $this->assertDatabaseHas('user_academic_contexts', [
                      'user_id' => LearningSliceSeeder::USER_ID,
                      'academic_track_id' => $targetId,
                      'status' => 'active',
                  ]);
              }

              public function test_fixture_tracks_remain_hidden_when_fixture_mode_is_disabled_without_assignment_logic(): void
              {
                  config(['modrik.fixture.enabled' => false]);

                  $registration = $this->postJson('/v1/auth/register', [
                      'name' => 'Year Scope Production Boundary Learner',
                      'email' => 'year-scope-production-boundary@example.test',
                      'password' => 'year-scope-production-boundary-password',
                  ])->assertCreated();
                  $token = (string) $registration->json('data.access_token');

                  $this->withToken($token)
                      ->getJson('/v1/academic-tracks')
                      ->assertOk()
                      ->assertJsonPath('data.tracks', []);

                  $realTrackId = '01J00000000000000000000058';
                  $this->createTrack($realTrackId, 'TEST:NONFIXTURE:YEAR-SCOPE', 'YEAR:GRADE-6:87654321', [
                      'ar' => 'مسار اختبار غير تجريبي',
                      'en' => 'Non-fixture test track',
                      'fr' => 'Parcours de test non-fixture',
                  ], false);

                  $this->withToken($token)
                      ->getJson('/v1/academic-tracks')
                      ->assertOk()
                      ->assertJsonPath('data.tracks.0.id', $realTrackId)
                      ->assertJsonPath('data.tracks.0.year.label', 'Grade 6');
              }

              /** @param array<string, string> $labels */
              private function createTrack(string $id, string $code, string $yearLevel, array $labels, bool $fixture = true): void
              {
                  $now = now();
                  DB::table('academic_tracks')->insert([
                      'id' => $id,
                      'code' => $code,
                      'board_reference' => null,
                      'syllabus_version' => null,
                      'year_level' => $yearLevel,
                      'title' => json_encode($labels, JSON_THROW_ON_ERROR | JSON_UNESCAPED_UNICODE),
                      'is_fixture' => $fixture,
                      'created_at' => $now,
                      'updated_at' => $now,
                  ]);
              }
          }
          '''.replace('\n          ', '\n').lstrip()
          write('apps/backend/tests/Feature/AcademicTrackCatalogueTest.php', backend_test)

          replace_once(
              'apps/web/src/lib/learning-api.ts',
              'export type AcademicTrack = {\n  id: string;\n  labels: Record<Locale, string>;\n};',
              'export type AcademicTrack = {\n  id: string;\n  year: { key: string; label: string };\n  labels: Record<Locale, string>;\n};',
          )

          selector = r'''"use client";

          import { useCallback, useEffect, useMemo, useState } from "react";
          import {
            learningApi,
            LearningApiError,
            type AcademicContext,
            type AcademicTrack,
            type Locale,
          } from "@/lib/learning-api";

          type CatalogueState = "loading" | "ready" | "empty" | "error" | "offline" | "permission";

          export const academicTrackCopy = {
            en: {
              label: "Academic track",
              yearLabel: "School year",
              yearHelp: "Choose your school year first. Every available track for that year will appear automatically.",
              authorized: "Choose your school year, then choose any academic track available for that year.",
              loading: "Loading school years and academic tracks…",
              empty: "No academic tracks are available for the configured school years right now.",
              error: "We couldn’t load school years and academic tracks. Nothing has changed. Try again.",
              offline: "Reconnect to load school years and academic tracks or change your track.",
              permission: "We can’t load academic tracks with this session. Sign in again, then try again.",
              retry: "Try again",
              activate: "Start with this track",
              change: "Change academic track",
              resetTitle: "Before you change academic track",
              resetBody: "Your previous academic track, attempts, and progress will be archived—not deleted. Any work still in progress may be left unfinished.",
              syncWarning: "Sync all pending answers and changes before you continue.",
              confirm: "I understand what will happen when I change tracks.",
              submitReset: "Change academic track",
              same: "Choose a different available track to continue.",
              busy: "Updating your academic track…",
              failed: "We couldn’t update your academic track. Nothing changed. Check your connection and that the selected track is still available, then try again.",
              resetRequired: "Nothing changed. Review what will happen when you change tracks, confirm it, then try again.",
            },
            ar: {
              label: "المسار الأكاديمي",
              yearLabel: "السنة الدراسية",
              yearHelp: "اختر السنة الدراسية أولًا. ستظهر تلقائيًا كل المسارات المتاحة لهذه السنة.",
              authorized: "اختر السنة الدراسية، ثم اختر أي مسار أكاديمي متاح لهذه السنة.",
              loading: "جارٍ تحميل السنوات الدراسية والمسارات الأكاديمية…",
              empty: "لا توجد مسارات أكاديمية متاحة للسنوات الدراسية المسجلة حاليًا.",
              error: "تعذر تحميل السنوات الدراسية والمسارات الأكاديمية. لم يتغير شيء. حاول مرة أخرى.",
              offline: "اتصل بالإنترنت لتحميل السنوات الدراسية والمسارات الأكاديمية أو تغيير مسارك.",
              permission: "لا يمكن تحميل المسارات الأكاديمية بهذه الجلسة. سجّل الدخول من جديد ثم حاول مرة أخرى.",
              retry: "حاول مرة أخرى",
              activate: "ابدأ بهذا المسار",
              change: "تغيير المسار الأكاديمي",
              resetTitle: "قبل تغيير المسار الأكاديمي",
              resetBody: "سيتم أرشفة مسارك الأكاديمي السابق ومحاولاتك وتقدّمك، ولن يتم حذفها. وقد يبقى أي عمل جارٍ غير مكتمل.",
              syncWarning: "زامن جميع الإجابات والتغييرات المعلّقة قبل المتابعة.",
              confirm: "أفهم ما سيحدث عند تغيير المسار.",
              submitReset: "تغيير المسار الأكاديمي",
              same: "اختر مسارًا آخر متاحًا للمتابعة.",
              busy: "جارٍ تحديث مسارك الأكاديمي…",
              failed: "تعذر تحديث مسارك الأكاديمي. لم يتغير شيء. تحقق من اتصالك ومن أن المسار المختار ما زال متاحًا، ثم حاول مرة أخرى.",
              resetRequired: "لم يتغير شيء. راجع ما سيحدث عند تغيير المسار وأكّد موافقتك، ثم حاول مرة أخرى.",
            },
            fr: {
              label: "Parcours académique",
              yearLabel: "Année scolaire",
              yearHelp: "Choisissez d’abord votre année scolaire. Tous les parcours disponibles pour cette année apparaîtront automatiquement.",
              authorized: "Choisissez votre année scolaire, puis n’importe quel parcours académique disponible pour cette année.",
              loading: "Chargement des années scolaires et des parcours académiques…",
              empty: "Aucun parcours académique n’est disponible pour les années scolaires configurées pour le moment.",
              error: "Nous n’avons pas pu charger les années scolaires et les parcours académiques. Rien n’a changé. Réessayez.",
              offline: "Reconnectez-vous pour charger les années scolaires et les parcours académiques ou changer de parcours.",
              permission: "Cette session ne permet pas de charger les parcours académiques. Reconnectez-vous à votre compte, puis réessayez.",
              retry: "Réessayer",
              activate: "Commencer avec ce parcours",
              change: "Changer de parcours académique",
              resetTitle: "Avant de changer de parcours académique",
              resetBody: "Votre ancien parcours académique, vos tentatives et votre progression seront archivés, pas supprimés. Tout travail en cours pourra rester inachevé.",
              syncWarning: "Synchronisez toutes les réponses et modifications en attente avant de continuer.",
              confirm: "Je comprends ce qui se passera quand je changerai de parcours.",
              submitReset: "Changer de parcours académique",
              same: "Choisissez un autre parcours disponible pour continuer.",
              busy: "Mise à jour de votre parcours académique…",
              failed: "Nous n’avons pas pu mettre à jour votre parcours académique. Rien n’a changé. Vérifiez votre connexion et que le parcours choisi est toujours disponible, puis réessayez.",
              resetRequired: "Rien n’a changé. Relisez ce qui se passera lors du changement de parcours, confirmez, puis réessayez.",
            },
          } as const;

          function operationStorageKey(action: "activate" | "reset", trackId: string) {
            return `modrik.academic-context.${action}.${trackId}`;
          }

          function stableOperationKey(action: "activate" | "reset", trackId: string) {
            const storageKey = operationStorageKey(action, trackId);
            const existing = window.localStorage.getItem(storageKey);
            if (existing) return existing;
            const value = `modrik-academic-${action}-${crypto.randomUUID()}`;
            window.localStorage.setItem(storageKey, value);
            return value;
          }

          function acknowledgeOperation(action: "activate" | "reset", trackId: string) {
            window.localStorage.removeItem(operationStorageKey(action, trackId));
          }

          export default function AcademicTrackSelector({
            context,
            locale,
            offline,
            onTransitioned,
          }: {
            context: AcademicContext | null;
            locale: Locale;
            offline: boolean;
            onTransitioned: () => Promise<void>;
          }) {
            const labels = academicTrackCopy[locale];
            const [state, setState] = useState<CatalogueState>(offline ? "offline" : "loading");
            const [tracks, setTracks] = useState<AcademicTrack[]>([]);
            const [selectedYearKey, setSelectedYearKey] = useState("");
            const [selectedId, setSelectedId] = useState("");
            const [confirmed, setConfirmed] = useState(false);
            const [busy, setBusy] = useState(false);
            const [message, setMessage] = useState("");

            const currentTrackId = context?.state === "active" ? context.academic_track_id : null;
            const selectedTrack = useMemo(
              () => tracks.find((track) => track.id === selectedId) ?? null,
              [selectedId, tracks],
            );
            const yearOptions = useMemo(() => {
              const options = new Map<string, string>();
              for (const track of tracks) {
                if (!options.has(track.year.key)) options.set(track.year.key, track.year.label);
              }
              return Array.from(options, ([key, label]) => ({ key, label }));
            }, [tracks]);
            const visibleTracks = useMemo(
              () => tracks.filter((track) => track.year.key === selectedYearKey),
              [selectedYearKey, tracks],
            );

            const loadCatalogue = useCallback(async () => {
              if (offline || !navigator.onLine) {
                setState("offline");
                return;
              }
              setState("loading");
              setMessage("");
              try {
                const nextTracks = await learningApi.academicTracks();
                setTracks(nextTracks);
                const currentTrack = nextTracks.find((track) => track.id === currentTrackId) ?? null;
                const preferredYear = currentTrack?.year.key ?? nextTracks[0]?.year.key ?? "";
                const preferredTracks = nextTracks.filter((track) => track.year.key === preferredYear);
                setSelectedYearKey(preferredYear);
                setSelectedId(currentTrack?.id ?? preferredTracks[0]?.id ?? "");
                setConfirmed(false);
                setState(nextTracks.length === 0 ? "empty" : "ready");
              } catch (error) {
                if (!navigator.onLine) {
                  setState("offline");
                } else if (error instanceof LearningApiError && (error.status === 401 || error.status === 403)) {
                  setState("permission");
                } else {
                  setState("error");
                }
              }
            }, [currentTrackId, offline]);

            useEffect(() => {
              const timer = window.setTimeout(() => void loadCatalogue(), 0);
              return () => window.clearTimeout(timer);
            }, [loadCatalogue]);

            async function applySelection() {
              if (!selectedTrack || offline || busy) return;
              const action = context?.state === "active" ? "reset" : "activate";
              if (action === "reset" && (selectedTrack.id === currentTrackId || !confirmed)) return;

              setBusy(true);
              setMessage("");
              try {
                const key = stableOperationKey(action, selectedTrack.id);
                if (action === "reset") {
                  await learningApi.resetAcademicContext(selectedTrack.id, key);
                } else {
                  await learningApi.activateAcademicContext(selectedTrack.id, key);
                }
                acknowledgeOperation(action, selectedTrack.id);
                await onTransitioned();
                await loadCatalogue();
              } catch (error) {
                if (error instanceof LearningApiError && error.code === "ACADEMIC_RESET_REQUIRED") {
                  setMessage(labels.resetRequired);
                } else {
                  setMessage(labels.failed);
                }
              } finally {
                setBusy(false);
              }
            }

            if (state !== "ready") {
              const stateMessage = {
                loading: labels.loading,
                empty: labels.empty,
                error: labels.error,
                offline: labels.offline,
                permission: labels.permission,
                ready: "",
              }[state];
              return (
                <div className="empty-panel" role={state === "error" || state === "permission" ? "alert" : "status"} aria-live="polite">
                  <p>{stateMessage}</p>
                  {state !== "loading" && (
                    <button type="button" className="secondary-button" disabled={offline} onClick={() => void loadCatalogue()}>
                      {labels.retry}
                    </button>
                  )}
                </div>
              );
            }

            const isReset = context?.state === "active";
            return (
              <div className="academic-track-selector">
                <p className="muted-copy">{labels.authorized}</p>
                <label className="text-answer-label">
                  <span>{labels.yearLabel}</span>
                  <select
                    value={selectedYearKey}
                    disabled={busy || offline}
                    onChange={(event) => {
                      const nextYearKey = event.target.value;
                      const nextTracks = tracks.filter((track) => track.year.key === nextYearKey);
                      const currentInYear = nextTracks.find((track) => track.id === currentTrackId);
                      setSelectedYearKey(nextYearKey);
                      setSelectedId(currentInYear?.id ?? nextTracks[0]?.id ?? "");
                      setConfirmed(false);
                      setMessage("");
                    }}
                  >
                    {yearOptions.map((year) => (
                      <option key={year.key} value={year.key}>{year.label}</option>
                    ))}
                  </select>
                  <small className="muted-copy">{labels.yearHelp}</small>
                </label>
                <label className="text-answer-label">
                  <span>{labels.label}</span>
                  <select
                    value={selectedId}
                    disabled={busy || offline || visibleTracks.length === 0}
                    onChange={(event) => {
                      setSelectedId(event.target.value);
                      setConfirmed(false);
                      setMessage("");
                    }}
                  >
                    {visibleTracks.map((track) => (
                      <option key={track.id} value={track.id}>{track.labels[locale]}</option>
                    ))}
                  </select>
                </label>

                {isReset && (
                  <div className="reset-consequence">
                    <h3>{labels.resetTitle}</h3>
                    <p>{labels.resetBody}</p>
                    <p>{labels.syncWarning}</p>
                    <label className="answer-option">
                      <input
                        type="checkbox"
                        checked={confirmed}
                        disabled={busy || selectedId === currentTrackId}
                        onChange={(event) => setConfirmed(event.target.checked)}
                      />
                      <span>{labels.confirm}</span>
                    </label>
                    {selectedId === currentTrackId && <p className="muted-copy">{labels.same}</p>}
                  </div>
                )}

                <button
                  type="button"
                  className="primary-button"
                  disabled={busy || offline || !selectedId || (isReset && (selectedId === currentTrackId || !confirmed))}
                  onClick={() => void applySelection()}
                >
                  {busy ? labels.busy : isReset ? labels.submitReset : labels.activate}
                </button>
                {message && <p className="inline-error" role="alert">{message}</p>}
              </div>
            );
          }
          '''.replace('\n          ', '\n').lstrip()
          write('apps/web/src/app/academic-track-selector.tsx', selector)

          replace_once(
              'apps/web/src/app/academic-track-navigation.test.tsx',
              '  assert.match(selector, /learningApi\\.academicTracks\\(\\)/);\n',
              '  assert.match(selector, /learningApi\\.academicTracks\\(\\)/);\n  assert.match(selector, /labels\\.yearLabel/);\n  assert.match(selector, /yearOptions\\.map/);\n  assert.match(selector, /visibleTracks\\.map/);\n  assert.match(selector, /track\\.year\\.key === nextYearKey/);\n',
          )
          replace_once(
              'apps/web/src/app/academic-track-selector-copy.test.tsx',
              '    assert.ok(labels.resetBody.length > 0);',
              '    assert.ok(labels.yearLabel.length > 0);\n    assert.ok(labels.yearHelp.length > 0);\n    assert.ok(labels.resetBody.length > 0);',
          )

          openapi = Path('docs/api/openapi.yaml')
          text = openapi.read_text(encoding='utf-8')
          replacements = [
              ('description: Backend-owned authorized academic-track catalogue and active academic context.', 'description: Backend-owned school-year-scoped academic-track catalogue, learner self-selection, and active academic context.'),
              ('summary: List academic tracks authorized for the current learner', 'summary: List school-year-scoped academic tracks available for learner self-selection'),
              ('operationId: listAuthorizedAcademicTracks', 'operationId: listAvailableAcademicTracks'),
              ('description: Returns only Backend-authorized learner choices in deterministic order. Opaque IDs are selection handles; board, syllabus, internal codes, year levels, authorization metadata, and eligibility rules are intentionally not exposed. Synthetic fixture tracks are never returned while fixture mode is disabled.', 'description: Returns every display-safe academic track currently configured for learner self-selection, grouped by an opaque school-year key supplied by the Backend. No per-user Admin track assignment is required. Internal board, syllabus, track codes, fixture markers, and operator metadata are not exposed. Synthetic fixture tracks are never returned while fixture mode is disabled.'),
              ('description: Authorized display-safe catalogue, which may be empty.', 'description: Display-safe year-scoped catalogue, which may be empty.'),
              ('description: Learner-specific catalogue responses must not be shared or reused after authorization changes.', 'description: Academic catalogue responses must not be shared or reused after catalogue changes.'),
              ('description: The academic_track_id must identify a currently authorized catalogue choice for this learner. The Backend remains authoritative for eligibility and active-context transition rules.', 'description: The academic_track_id must identify a currently available display-safe catalogue choice. The selected track carries its Backend-owned school-year scope; no per-user Admin assignment is required. The Backend remains authoritative for active-context transition rules.'),
              ('description: The target academic_track_id must remain a currently authorized catalogue choice for this learner. Existing archival, history-preservation, and different-track rules remain Backend-owned.', 'description: The target academic_track_id must remain a currently available display-safe catalogue choice. The selected track carries its Backend-owned school-year scope; no per-user Admin assignment is required. Existing archival, history-preservation, and different-track rules remain Backend-owned.'),
          ]
          for old, new in replacements:
              if old not in text:
                  raise SystemExit(f'OpenAPI expected text missing: {old}')
              text = text.replace(old, new, 1)
          schema_old = """    AcademicTrack:\n      type: object\n      additionalProperties: false\n      required: [id, labels]\n      properties:\n        id: {$ref: '#/components/schemas/Ulid'}\n        labels: {$ref: '#/components/schemas/AcademicTrackLabels'}"""
          schema_new = """    AcademicTrack:\n      type: object\n      additionalProperties: false\n      required: [id, year, labels]\n      properties:\n        id: {$ref: '#/components/schemas/Ulid'}\n        year:\n          type: object\n          additionalProperties: false\n          required: [key, label]\n          properties:\n            key: {type: string, minLength: 1, maxLength: 160}\n            label: {type: string, minLength: 1, maxLength: 160}\n        labels: {$ref: '#/components/schemas/AcademicTrackLabels'}"""
          if schema_old not in text:
              raise SystemExit('OpenAPI AcademicTrack schema block missing')
          openapi.write_text(text.replace(schema_old, schema_new, 1), encoding='utf-8')

          replace_once(
              'docs/requirements/requirements-index.yaml',
              'updated: 2026-08-22',
              'updated: 2026-08-23',
          )
          replace_once(
              'docs/requirements/requirements-index.yaml',
              '  - id: REQ-P0-002\n    title: Locked active academic context\n    statement: Onboarding establishes an active academic track; a full reset archives historical mastery and attempts rather than deleting them.\n    area: academics\n    priority: P0\n    acceptance_criteria: [AC-P0-010]\n    status: indexed',
              '  - id: REQ-P0-002\n    title: Year-scoped learner-selected academic context\n    statement: Onboarding lets the learner choose a school year and then any display-safe academic track configured for that year without per-user Admin assignment; once active, a full reset archives historical mastery and attempts rather than deleting them.\n    area: academics\n    priority: P0\n    acceptance_criteria: [AC-P0-010, AC-P0-022]\n    status: owner_authorized_2026_08_23',
          )
          replace_once('docs/requirements/acceptance-criteria.yaml', 'updated: 2026-08-22', 'updated: 2026-08-23')
          ac = Path('docs/requirements/acceptance-criteria.yaml')
          text = ac.read_text(encoding='utf-8')
          anchor = "  - id: AC-P0-011\n"
          if anchor not in text:
              raise SystemExit('AC-P0-011 anchor missing')
          addition = "  - id: AC-P0-022\n    requirement_ids: [REQ-P0-002]\n    statement: An authenticated learner can choose a school year and then any display-safe academic track configured for that year without a per-user Admin grant; the student surface refreshes the track choices automatically when the chosen year changes, while fixture tracks remain hidden outside fixture mode.\n    verification: backend catalogue/domain tests plus Student Web year-to-track regression coverage\n"
          text = text.replace(anchor, addition + anchor, 1)
          ac.write_text(text, encoding='utf-8')

          replace_once(
              'docs/product/MASTER_PLAN_START_HERE.md',
              '- Active Academic Track is locked after onboarding; full reset archives history rather than deleting mastery/attempts.',
              '- Academic onboarding is learner-driven: the learner chooses a school year and then any display-safe track configured for that year, with no per-user Admin track assignment. Once a track is active, changing track or year uses the full reset flow and archives history rather than deleting mastery/attempts.',
          )

          matrix = Path('docs/product/capability-surface-matrix.yaml')
          text = matrix.read_text(encoding='utf-8')
          text = text.replace('updated: 2026-08-22', 'updated: 2026-08-23', 1)
          old_note = '    notes: Integrated by PR #209. A first-class Student navigation destination shows the current context and Backend-authorized track catalogue, then reuses the existing explicit activate/reset confirmation flow. Prior attempts/progress remain archived, no mastery or answers migrate across tracks, and track IDs are never typed by the learner.'
          new_note = '    notes: PR #209 supplied the discoverable reset/archive workspace; owner directive #305 changes catalogue eligibility to learner self-selection by school year. Student Web chooses a year first and automatically shows every display-safe track configured for that year, with no per-user Admin track assignment. Existing explicit activate/reset confirmation, history archival, no mastery/answer migration, and opaque track-ID handling remain authoritative.'
          if old_note not in text:
              raise SystemExit('capability matrix academic note missing')
          matrix.write_text(text.replace(old_note, new_note, 1), encoding='utf-8')

          dd = Path('docs/data/data-dictionary.md')
          text = dd.read_text(encoding='utf-8')
          text = text.replace('P0-ACADEMIC-CONTRACT-002 learner-authorized track catalogue', 'P0-ACADEMIC-YEAR-SCOPE year-scoped learner self-selection', 1)
          old_row = '| `academic_tracks` | Backend-owned curriculum/board/syllabus/year combination. | Unique stable internal `code`; board and syllabus may be null in fixtures only and cannot be guessed for real content. Catalogue responses expose only the stable opaque ULID plus validated AR/EN/FR display labels; internal code, board, syllabus, year and fixture metadata remain server-side. Issue #19 publication consumes an existing track and does not create or synthesize one. |'
          new_row = '| `academic_tracks` | Backend-owned curriculum/board/syllabus/year combination and source of learner-selectable tracks. | Unique stable internal `code`; board and syllabus may be null in fixtures only and cannot be guessed for real content. Catalogue responses expose the stable opaque ULID, an opaque `year.key`, a safe readable `year.label`, and validated AR/EN/FR track labels. Internal track code, board, syllabus and fixture metadata remain server-side. Issue #19 publication consumes an existing track and does not create or synthesize one. |'
          if old_row not in text:
              raise SystemExit('data dictionary academic_tracks row missing')
          text = text.replace(old_row, new_row, 1)
          old_auth = '| `academic_track_authorizations` | Backend-owned allowlist of track choices currently available to one learner. | Unique `(user_id, academic_track_id)`; `revoked_at IS NULL` is the active authorization state; `sort_order` plus track ULID defines deterministic catalogue ordering. Authorization is never client writable. Fixture authorizations may be seeded only while fixture mode is enabled, and fixture tracks are filtered from production-mode catalogue reads even if stale fixture rows exist. |'
          new_auth = '| `academic_track_authorizations` | Legacy compatibility table from the superseded per-user assignment model. | Issue #305 removes this table from Student catalogue/activate/reset authority. Existing rows are retained temporarily for backward migration/history compatibility only; no learner requires an Admin-created row to see or choose a track. A follow-up migration may remove the table after all residual fixtures/tests/consumers are proven absent. |'
          if old_auth not in text:
              raise SystemExit('data dictionary authorization row missing')
          text = text.replace(old_auth, new_auth, 1)
          start = text.index('## Authorized academic-track catalogue contract')
          end = text.index('## Production authentication lifecycle contract', start)
          replacement = '''## Year-scoped academic-track self-selection contract\n\nThe catalogue is a Backend-owned read model over `academic_tracks`, not a per-user assignment table. Any authenticated learner may see every display-safe non-fixture track that the Backend currently considers available. Each choice exposes an opaque track `id`, a Backend-derived `year.key`/readable `year.label`, and complete safe `labels.ar`, `labels.en`, and `labels.fr`. Student clients group by `year.key`: the learner chooses a school year first, then chooses any track in that year.\n\nMissing/unsafe year or track labels fail closed. Fixture rows remain unavailable when fixture mode is disabled. Internal board, syllabus, track code and fixture metadata do not leave the Backend. There is no Admin-to-student assignment step in the selection path.\n\nActivation and reset validate the target through the same available-track source. Existing active-context, different-track, archival, history-preservation, idempotency and outbox semantics remain authoritative. The selected track itself fixes the canonical `year_level`, so clients do not invent or type an academic identifier.\n\nProduction track definitions remain owner/content-managed input. The repository does not invent real board, syllabus, version or year values. `academic_track_authorizations` is legacy compatibility state only and is no longer runtime selection authority.\n\n'''
          text = text[:start] + replacement + text[end:]
          text = text.replace('- Academic-track authorization: active when `revoked_at` is null; revoked rows remain durable and unavailable to catalogue/activation/reset.', '- Academic-track authorization: legacy compatibility state only after Issue #305; it is not consulted by catalogue/activation/reset and should receive no new product dependency.', 1)
          dd.write_text(text, encoding='utf-8')

          tasks = Path('TASKS.md')
          text = tasks.read_text(encoding='utf-8')
          anchor = '## Control plane / release\n'
          if anchor not in text:
              raise SystemExit('TASKS control-plane anchor missing')
          section = '''## Academic year-scoped self-selection / Issue #305\n\n- [ ] #305 — replace per-user academic-track assignment with Backend-owned year-scoped learner self-selection; Student Web chooses **school year → track**, while reset/archive history authority remains unchanged.\n- [ ] Follow-up gap — add an explicit Backend-authoritative `academic_tracks` availability lifecycle (`draft/published/retired` or approved equivalent) plus discoverable audited Admin control. The current schema has no track publication/active field, so #305 can only filter fixture/display safety, not operator availability.\n- [ ] Follow-up gap — add Mobile Year → Track UX parity. The current Mobile parser remains wire-compatible because it ignores the new `year` field, but it does not yet expose the owner-approved year-first selection flow.\n- [ ] Follow-up gap — retire the legacy `academic_track_authorizations` table after repository-wide consumer/fixture verification; #305 removes it from runtime selection authority but deliberately avoids a destructive migration in the same product-contract change.\n- [ ] Follow-up gap — define canonical localized school-year metadata and operator-controlled track display order. #305 safely derives a readable year label and deterministic ordering from current track data, but full AR/EN/FR year naming and curated ordering are not represented in the existing schema.\n\n'''
          tasks.write_text(text.replace(anchor, section + anchor, 1), encoding='utf-8')

          state = Path('CURRENT_STATE.md')
          text = state.read_text(encoding='utf-8')
          anchor = '## Repository-verifiable work queue at this checkpoint\n'
          if anchor not in text:
              raise SystemExit('CURRENT_STATE queue anchor missing')
          addition = '''## Owner-directed academic selection change\n\nIssue #305 records the 2026-08-23 owner decision that academic-track eligibility is school-year scoped rather than assigned per learner. The implementation candidate changes the Backend catalogue from `academic_track_authorizations` to display-safe `academic_tracks`, adds year metadata, and makes Student Web choose **year → track** while preserving the existing full reset/archive transition.\n\nGap audit from this contract change is explicit: `academic_tracks` still lacks a publication/availability lifecycle; Mobile still needs year-first UX parity; the old authorization table needs a later non-destructive retirement after residual-consumer proof; and canonical localized school-year labels / operator-curated track ordering need dedicated metadata rather than inferred references.\n\n'''
          state.write_text(text.replace(anchor, addition + anchor, 1), encoding='utf-8')

          changelog = Path('CHANGELOG.md')
          text = changelog.read_text(encoding='utf-8')
          heading = '# CHANGELOG\n\n'
          if not text.startswith(heading):
              raise SystemExit('CHANGELOG heading missing')
          entry = '''## 2026-08-23 — owner-directed year-scoped academic self-selection (#305)\n\n- Owner product authority supersedes the per-user academic-track eligibility model from Issue #21: students choose a school year and then any display-safe track configured for that year; Admin does not assign tracks to individual learners.\n- Backend catalogue/activation/reset authority moves to `academic_tracks` with fixture/display-safety gates, while `user_academic_contexts` remains the learner choice/history record and full reset continues to archive attempts/progress rather than delete or migrate them.\n- Student Web adds a first-class Year → Track selector and automatically rebuilds the visible track choices when the learner changes year.\n- Gap audit is recorded in `TASKS.md` / `CURRENT_STATE.md`: track publication/availability lifecycle, Mobile year-first parity, legacy authorization-table retirement, and canonical localized year/display-order metadata remain explicit follow-ups rather than hidden assumptions.\n\n'''
          changelog.write_text(heading + entry + text[len(heading):], encoding='utf-8')

          # Static guard: runtime selection must not retain a per-user authorization table dependency.
          runtime_paths = [
              Path('apps/backend/app/Services/AcademicTrackCatalogueService.php'),
              Path('apps/backend/app/Services/AcademicContextService.php'),
              Path('apps/backend/app/Http/Controllers/Api/AcademicContextController.php'),
          ]
          for runtime_path in runtime_paths:
              if 'academic_track_authorizations' in runtime_path.read_text(encoding='utf-8'):
                  raise SystemExit(f'legacy authorization dependency remains in runtime path {runtime_path}')
          PY

          rm .github/workflows/issue-305-apply-patch-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "P0 Academic: year-scoped learner self-selection (#305)"
          git push origin HEAD:issue-305-year-scoped-self-selection
